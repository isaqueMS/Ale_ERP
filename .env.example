# Este projeto não usa variáveis de ambiente hoje: a config do Firebase
# fica em firebase-applet-config.json (ver src/lib/firebase.ts).
# Este arquivo foi deixado vazio de propósito — as variáveis GEMINI_API_KEY
# e APP_URL que existiam aqui eram resquício de outro scaffold e não eram
# usadas em nenhum lugar do src/.
